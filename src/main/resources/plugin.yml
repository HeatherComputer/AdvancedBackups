name: AdvancedBackups
version: ${version}
main: computer.heather.AdvancedBackups.AdvancedBackups
commands:
   backup:
      description: Commands for Advanced Backups.
      usage: /backup start | reload-config | reload-client-config | snapshot | reset-chain
      permission: advancedbackups.backup 